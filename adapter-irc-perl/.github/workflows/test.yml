name: run-tests

on:
  push:
    branches: [main]
    paths:
      - '.github/workflows/test.yml'
      - '.perlcriticrc'
      - '.perltidyrc'
      - 'lib/**'
      - 'Makefile.PL'
      - 't/**'
      - 'xt/**'
  pull_request:
    branches: [main]
    paths:
      - '.github/workflows/test.yml'
      - '.perlcriticrc'
      - '.perltidyrc'
      - 'lib/**'
      - 'Makefile.PL'
      - 't/**'
      - 'xt/**'

permissions:
  contents: read

jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        perl: ['5.40', 'latest']
    name: Perl ${{ matrix.perl }}
    env:
      PERL5LIB: /home/runner/perl5/lib/perl5:${{ github.workspace }}/core-perl/lib
      PERL_LOCAL_LIB_ROOT: /home/runner/perl5
      PERL_MB_OPT: --install_base /home/runner/perl5
      PERL_MM_OPT: INSTALL_BASE=/home/runner/perl5

    steps:
      - uses: actions/checkout@v4
        with:
          path: adapter-irc-perl

      - uses: actions/checkout@v4
        with:
          repository: overnet-project/core-perl
          path: core-perl

      - uses: actions/checkout@v4
        with:
          repository: overnet-project/spec
          path: spec

      - uses: shogo82148/actions-setup-perl@v1
        id: setup-perl
        with:
          perl-version: ${{ matrix.perl }}

      - uses: actions/cache@v4
        with:
          path: ~/perl5
          key: perl-${{ steps.setup-perl.outputs.perl-version }}-${{ hashFiles('adapter-irc-perl/Makefile.PL', 'adapter-irc-perl/.github/workflows/test.yml') }}

      - name: Install dependencies
        run: |
          cpanm --local-lib ~/perl5 --installdeps ./core-perl
          cpanm --local-lib ~/perl5 --installdeps ./adapter-irc-perl
          cpanm --local-lib ~/perl5 --reinstall https://github.com/overnet-project/overnet-perl-style/archive/refs/heads/main.tar.gz

      - name: Run tests
        working-directory: adapter-irc-perl
        run: |
          prove -r -l -v t/
          prove -r -l -v xt/author/

  devel-cover:
    runs-on: ubuntu-latest
    name: Devel::Cover gate
    timeout-minutes: 90
    env:
      PERL5LIB: /home/runner/perl5/lib/perl5:${{ github.workspace }}/core-perl/lib
      PERL_LOCAL_LIB_ROOT: /home/runner/perl5
      PERL_MB_OPT: --install_base /home/runner/perl5
      PERL_MM_OPT: INSTALL_BASE=/home/runner/perl5

    steps:
      - uses: actions/checkout@v4
        with:
          path: adapter-irc-perl

      - uses: actions/checkout@v4
        with:
          repository: overnet-project/core-perl
          path: core-perl

      - uses: actions/checkout@v4
        with:
          repository: overnet-project/spec
          path: spec

      - uses: shogo82148/actions-setup-perl@v1
        with:
          perl-version: '5.40'

      - uses: actions/cache@v4
        with:
          path: ~/perl5
          key: perl-devel-cover-5.40-${{ hashFiles('adapter-irc-perl/Makefile.PL', 'adapter-irc-perl/.github/workflows/test.yml') }}

      - name: Install dependencies
        run: |
          cpanm --local-lib ~/perl5 --installdeps ./core-perl
          cpanm --local-lib ~/perl5 --installdeps ./adapter-irc-perl
          cpanm --local-lib ~/perl5 --reinstall https://github.com/overnet-project/overnet-perl-style/archive/refs/heads/main.tar.gz
          cpanm --local-lib ~/perl5 --notest Devel::Cover
          echo "$HOME/perl5/bin" >> "$GITHUB_PATH"

      - name: Enforce per-file coverage floors
        working-directory: adapter-irc-perl
        env:
          OVERNET_COVERAGE: '1'
        run: prove -l -v xt/author/devel-cover.t
